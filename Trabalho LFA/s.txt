load 'question.qst' as q;

//Teste linear

Quiz Teste(partcipant, score) {

    /*//60% para parte1
    //4 perguntas sobre historia de portugal
    //25% cada pergunta
    Section parte1 (
        => P1;
        => P2:[0,1,2];
        => P4;
        => P5;
    );

    //40% para parte2
    //2 perguntas sobre gramatica
    //50% cada pergunta
    Section parte2 (
        => P7;
        => P8:[0,3];
    );*/
}

main {
    
    /*Quiz t;
    t = Teste("participant",100);
    
    //comecar na primeira parte pergunta P1
    goto t.parte1;
    goto parte1.P1;
    //ou
    //goto t.parte1.P1;

    int p1Size;
    p1Size = sizeof parte1;
    int i;
    i = 0;
    
    int ResultP1;
    ResultP1 = 0;

    while(i < p1Size) { 
        //responder pergunta atual
        show t.current.current;
        answer(t.current.current);
        
        //calcular resultado
        int resultQuestion;
        resultQuestion = result t.current.current;
        resultP1 = resultP1 + 0.25 * resultQuestion;

        //avancar para a proxima pergunta
        next t.current;
        i++;
    }
    
    //avancar para proxima Section (parte2), comecar na pergunta P7
    next t;
    goto t.parte2.P7;
    //ou simplesmente
    //goto parte2.P7
    
    i = 0;
    int p2Size;
    p2Size = sizeof parte2;

    int resultP2;
    resultP2 = 0;

    while(i < p2size) {
        show t.current.current;
        answer(t.current.current);
        i++;
        
        //calcular resultado
        int resultQuestion;
        resultQuestion = result t.current.current;
        resultP2 = resultP2 + 0.5 * resultQuestion;
    }

    int finalResult;
    finalResult = resultP1 * 0.6 + resultP2 * 0.4; 

    if(finalResult < 50) {
        print "Reprovado ! \n";
    }
    else {
        print "Aprovado ! \n";
    }

    print "Resultado final : " + finalResult;*/
}
